<UserControl x:Class="MyBusiness.Views.AddProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyBusiness.Views"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        
        <Button Style="{StaticResource addImageButton}"
                Grid.Row="3"
                Tag="{StaticResource color1}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Add picture" Style="{StaticResource addImageButtonText}"/>
            </StackPanel>
        </Button>
        
        <Button x:Name="addPicture"
                Background="#363636"
                BorderBrush="{StaticResource color4}"
                Grid.Column="1">
            <fa:IconImage Icon="Add" Foreground="#818181"/>
        </Button>
        <TextBlock Text="Add picture"
                   Foreground="{StaticResource plainTextColor1}"
                   FontSize="16"
                   FontFamily="Montserrat"
                   FontWeight="Medium"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Grid.Column="0"
                   Grid.Row="4"/>
    </Grid>
</UserControl>
