<UserControl x:Class="MyBusiness.Views.AddProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyBusiness.Views"
             xmlns:viewModel="clr-namespace:MyBusiness.ViewModels"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Rectangle Grid.Row="0" Grid.RowSpan="3"
                   Height="150"
                   Width="150"
                   RadiusX="5"
                   RadiusY="5">
            <Rectangle.Fill>
                <ImageBrush ImageSource="/Images/DefaultImage.jpg"/>
            </Rectangle.Fill>
        </Rectangle>

        <Button x:Name="addPicture" 
                Style="{StaticResource addImageButton}"
                Grid.Row="3"
                Click="addPicture_Click">

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Add picture" Style="{StaticResource addImageButtonText}"/>
            </StackPanel>

        </Button>

        <Button Style="{StaticResource addImageButton}"
                Grid.Row="4"
                Tag="{StaticResource color1}"
                Command="{Binding AddProductViewModel}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Add picture 2" Style="{StaticResource addImageButtonText}"/>
            </StackPanel>
        </Button>

    </Grid>
</UserControl>
